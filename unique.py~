def die(k):
	k += 1

a = [1,4,4,3,5,6,6]
b = []
for i in a:#iterate over 'a'
	count = 0
	for j in a:#iterate over 'b'
		if i == j:
			count = count+1
			#for k in b: #iterate over list 'b' for memebers repeating more than 2
			#	if k == j:
			#		print"is already a member of b"
			#	else:
			#		print"append to list b"
			#		b.append(i)
			#		break
		else:
			die(0)
			
	if count >= 2:
		
		b.append(i)
	else:
		print i,

	
print b#unique function using 'set'
x= set(b)
print"Duplicate elements:\n"
for i in x:
	print i,



